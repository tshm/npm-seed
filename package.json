{
  "name": "npm-seed",
  "version": "0.0.0",
  "description": "seed / template folder file structure for npm project (with gruntjs + coffeescript). ",
  "main": "lib/main",
  "bin": {
    "npm-seed": "./bin/npm-seed"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "tosh shimayama",
  "license": "BSD",
  "devDependencies": {
    "grunt-contrib-coffee": "~0.7.0",
    "grunt-contrib-watch": "~0.3.1",
    "grunt": "~0.4.1"
  }
}
